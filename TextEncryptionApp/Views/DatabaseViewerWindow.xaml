<Window x:Class="TextEncryptionApp.Views.DatabaseViewerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Database Viewer" Height="450" Width="800">
    <Grid>
        <DataGrid x:Name="dataGrid" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" AutoGenerateColumns="True"/>
        <Button Content="Refresh" HorizontalAlignment="Right" VerticalAlignment="Bottom" Click="RefreshButton_Click" Margin="10"/>
    </Grid>
</Window>

